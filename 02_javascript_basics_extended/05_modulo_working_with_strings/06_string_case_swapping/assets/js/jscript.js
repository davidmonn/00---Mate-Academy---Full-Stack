const palavra = 'Ola, mundo!';

const palavraMaiuscula = palavra.toUpperCase(palavra);
const palavraMinuscula = palavra.toLowerCase(palavra);

console.log(palavra);
console.log(palavraMaiuscula);
console.log(palavraMinuscula);
