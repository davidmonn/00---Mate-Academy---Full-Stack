const s1 = '<div class="spinner">Carregando...</div>';

console.log(s1);

const a = 2;
const b = 5;

const s2 = `
<h1>Titulo</h1>
<ul>
    <li>${a} + ${b} = ${a + b}</li>
    <li>${a} * ${b} = ${a * b}</li>
    <li>${a} / ${b} = ${a / b}</li>
</ul>
`;

console.log(s2);